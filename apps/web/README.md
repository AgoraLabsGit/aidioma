# Production web app — reserved for A1

The greenfield Next.js application will be scaffolded here in wave A1. A0 repository cleanup may
prepare shared packages and tooling, but must not add application implementation here.

Current application design authority: `Docs/`.
Shared lesson contract: `@aidioma/lesson-schema` in `packages/lesson-schema/`.
