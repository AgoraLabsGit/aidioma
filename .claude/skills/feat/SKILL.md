---
name: feat
description: Deprecated. /feat was removed in Development System V2; use /run instead.
---

# /feat (removed)

`/feat` is retired. Use `/run` to start or resume the active phase.

Tell the operator to use `/run` and follow `.claude/skills/run/SKILL.md`.
