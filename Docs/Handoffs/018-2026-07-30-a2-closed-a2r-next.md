---
title: Handoff — A2 closed; A2R or batch SHIP next
type: handoff
status: active
updated: 2026-07-30
---

# Handoff — A2 closed; A2R or batch SHIP next

**Role:** sole App release coordinator; preserve the cumulative lineage and all worktrees
**Next command:** `/run` for A2R, or explicit `SHIP` to release A2 and consolidate first
**Fresh-session prompt:** `Continue AIdioma from Handoff 018; run A2R unless Mike chooses SHIP.`

## Closed Preview position

- A2 is development-closed and queued in ROADMAP's cumulative batch. The tested app candidate is
  `9cdca857f626685bf07129c42e6ddf770d16a5e2` on `release/A2-2026-07-29`, PR #2, immutable Preview
  `https://aidioma-bcfc7v6t4-agoralabs.vercel.app`.
- Local cache-free App gates passed at 19 files / 140 tests, build, and smoke; Content typecheck,
  validation, contract smoke, 18 fixtures, prototype check, and hosted PR checks passed. The known
  production audit remains 4 findings (1 moderate, 3 high), no compatible fix, owned by OI-026.
- Authenticated exact/close/AI, spoof rejection, signed-out 401, and a fresh 30-success/one-429 burst
  passed. Gateway generation, opaque user/tag reporting, and the active $1 evaluation-key budget
  were evidenced. BUG-001 is closed; `quotaEntityId` was the rejected option and is regression-forbidden.
- A2 stays stateless and has no write path. A1-H's isolated Preview DB receipt remains authoritative;
  Sensitive deployment credentials were not extracted for a redundant post-call hash query.
- No Production config, `main` merge, branch deletion, PR closure, or worktree cleanup occurred.

## Two valid next paths

1. `/run`: open A2R from the cumulative release lineage, perform the application baseline/founder UI
   review, then `/close` it into the same Preview batch. Existing worktrees remain preserved.
2. `SHIP`: reconfirm the exact final batch, apply only the reviewed Production-conditioned Firewall
   rule, merge PR #2, prove Production, then run the strict containment cleanup in Handoff 017.

## Cleanup invariant

- Do not remove any current worktree or branch before successful `SHIP`. After `origin/main` contains
  the batch, run `.claude/skills/close/scripts/preflight.sh --fetch --target origin/main --cleanup-audit`.
- Only `SAFE_REMOVE_AFTER_SHIP` worktrees/branches may be removed. Finish with one clean primary
  worktree on local `main` matching `origin/main`, PR #1 closed as superseded, and contained remote
  worker/release branches deleted.
