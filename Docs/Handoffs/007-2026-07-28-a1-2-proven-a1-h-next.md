---
title: Handoff — A1-2 proven; A1-H next
type: handoff
status: superseded
updated: 2026-07-28
---

# Handoff — A1-2 proven; A1-H next

**Role:** primary App-lane coordinator; Content lane remains independently active
**Operator:** Mike; continue autonomously and ask only for genuine blockers
**Rule:** never push without Mike's explicit GO after `/close`

## Position

- A1-2 is proven locally and against real Neon: two journaled migrations, 4 unique lesson slugs,
  134 unique authored item rows, 4 deprecated items retained, and repeated seeds change zero rows.
- Vercel project `agoralabs/aidioma` is connected to `AgoraLabsGit/aidioma`, rooted at `apps/web`
  on Node 22.x. Free Neon `aidioma-db` is attached to all environments. No deployment exists yet.
- Production Clerk is not configured; both Clerk keys plus the four documented route variables are
  still required together before auth/deployment verification.
- `OI-026` remains open for upstream Next/PostCSS/sharp and ESLint advisories. Do not force-upgrade.
- Two operator/tool-owned unstaged files remain outside A1-2: `apps/web/.gitignore` adds `/.clerk/`,
  and `apps/web/next-env.d.ts` points at dev route types. Preserve them.

## Proof and review

- App gates: typecheck, lint, 20 tests, credential-free build, and production smoke PASS.
- Content gates: validator 0 errors, contract 13/13, fixtures 18/18, prototype current.
- Database: first seed changed 138 rows; all repeat runs changed 0. Migration journal/checksum and
  deferred ordinal uniqueness were added during review; all audit/review deltas PASS.
- Local Node is 22.18.0 and prints an engine warning below the repo floor; Vercel and CI are set to
  Node 22.x / 22.22.2 respectively.

## Next actions

1. Run A1-H: residue scan, execute any fired deprecations, recheck OI-026, and reconcile A1 specs.
2. Run the cache-free full app/content suites and high-effort whole-wave review for `/close`.
3. Give Mike the A1 recap and click-by-click human test; push only after VERIFIED + explicit GO.
4. In the independent Content lane, a1-05 remains the next lesson and must reach validator zero
   errors plus independent L2 PASS before a1-06.
