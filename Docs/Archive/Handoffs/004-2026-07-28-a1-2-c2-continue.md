---
title: Handoff — A1-2 seed and C2 lesson production
type: handoff
status: superseded
updated: 2026-07-28
---

# Handoff — A1-2 seed and C2 lesson production

**Role:** primary coordinator for parallel app/content development  
**Operator:** Mike; continue autonomously and ask only for genuine blockers  
**Rule:** never push without Mike's explicit GO at wave close

## Boot

1. `Docs/STATE.md` → `Docs/ROADMAP.yaml` → `Docs/INDEX.md`
2. This handoff
3. `Docs/Waves/A1-1-nextjs-scaffold.md` + `Docs/Waves/C2-1-draft-launch-lessons.md`
4. `Docs/Specs/Areas/platform.md` + `content-pipeline.md` + `data-model.md`
5. `Docs/Registers/open-items.md` (especially OI-026) + `deprecations.md`
6. `.claude/skills/run/SKILL.md` before the next slice

## Position

- Local baseline exists on `main`; there is no remote and nothing has been pushed.
- A1-1 is closed/proven. The responsive Next.js shell, keyless-safe Clerk boundary, lazy
  server-only Neon + Drizzle scaffold, root workspace, and app gates pass. Proof screenshot:
  `apps/web/artifacts/a1-shell-mobile.png`.
- A1 remains active; A1-2 is next: import the canonical shared lesson contract and build an
  idempotent Neon seed keyed by lesson slug. Do not redeclare schema or require credentials to build.
- C2-1 remains active. OI-025 is closed. a1-04 is merged and L2-PASS (r3); the next content unit is
  a1-05, using the same draft → deterministic gates → independent L2 → fix/re-QA loop.
- OI-026 owns current npm advisories with no compatible upstream fix. Recheck on dependency
  releases and before deployment; do not use `npm audit fix --force` or unsupported overrides.

## Integrated checkpoints

- A1 app merge `8b7d535`; workspace integration `7923c94`; Node-floor audit fix `935c2b1`;
  A1 proof record `051b7bb`.
- C2 OI-025 merge `891c1c5`; a1-04 merge `b96097e`; prototype export `38dc0af`.
- Latest merged gates: app typecheck/lint/test (8/8)/build/smoke PASS; content typecheck PASS;
  corpus validation 0 errors across four lessons; contract smoke 13/13; fixtures 18/18;
  prototype export current.

## Exact next actions

1. Open `A1-2` wave record and isolated `slice/A1-2` branch/worktree. Implement only shared-contract
   import, content CI, idempotent Neon upsert-by-slug seed, and Lesson 0 migration/proof.
2. Continue `slice/C2-1` with a1-05 only. Preserve cumulative-vocabulary discipline and require a
   separate independent L2 reviewer before merge.
3. Coordinator owns root workspace/lockfile, ROADMAP/STATE/registers, generated prototype export,
   cross-lane merges, and full merged gates.
4. Do not start A2 or a1-06 until their immediately preceding unit is proven and recorded.
5. Never inspect or stage ignored `Archive/Legacy-Apps/`; never configure or push a remote without
   explicit authorization.

## Known non-blockers

- The shell intentionally shows truthful zero-data states until A1-2 seeds/loads lessons.
- Current-page `aria-current` is dispositioned to the existing A4-2 navigation/layout cleanup.
- The preferred in-app browser runtime was unavailable; repository-required Playwright production
  smoke is the authoritative browser proof and saves the screenshot above.
